'use client';
import { useState, useEffect } from 'react';

const useIsServer = () => {
  const [isServer, setServer] = useState(true);
  useEffect(() => {
    setServer(false);
  }, []);
  return isServer;
};

export { useIsServer };

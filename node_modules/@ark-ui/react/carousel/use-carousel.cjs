'use client';
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const carousel = require('@zag-js/carousel');
const react$1 = require('@zag-js/react');
const react = require('react');
require('../environment/index.cjs');
const useEvent = require('../use-event.cjs');
const environmentContext = require('../environment/environment-context.cjs');

function _interopNamespaceDefault(e) {
  const n = Object.create(null, { [Symbol.toStringTag]: { value: 'Module' } });
  if (e) {
    for (const k in e) {
      if (k !== 'default') {
        const d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: () => e[k]
        });
      }
    }
  }
  n.default = e;
  return Object.freeze(n);
}

const carousel__namespace = /*#__PURE__*/_interopNamespaceDefault(carousel);

const useCarousel = (props = {}) => {
  const initialContext = {
    id: react.useId(),
    getRootNode: environmentContext.useEnvironmentContext(),
    index: props.defaultIndex,
    ...props
  };
  const context = {
    ...initialContext,
    index: props.index,
    onIndexChange: useEvent.useEvent(props.onIndexChange, { sync: true })
  };
  const [state, send] = react$1.useMachine(carousel__namespace.machine(initialContext), { context });
  return carousel__namespace.connect(state, send, react$1.normalizeProps);
};

exports.useCarousel = useCarousel;

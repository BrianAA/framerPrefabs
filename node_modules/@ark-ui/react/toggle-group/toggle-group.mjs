'use client';
import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { forwardRef } from 'react';
import { createSplitProps } from '../create-split-props.mjs';
import { ark } from '../factory.mjs';
import { ToggleGroupProvider } from './toggle-group-context.mjs';
import { useToggleGroup } from './use-toggle-group.mjs';

const ToggleGroup = forwardRef((props, ref) => {
  const [toggleGroup, localProps] = createSplitProps()(props, [
    "defaultValue",
    "dir",
    "disabled",
    "getRootNode",
    "id",
    "ids",
    "loop",
    "multiple",
    "onValueChange",
    "orientation",
    "rovingFocus",
    "value"
  ]);
  const api = useToggleGroup(toggleGroup);
  const mergedProps = mergeProps(api.rootProps, localProps);
  return /* @__PURE__ */ jsx(ToggleGroupProvider, { value: api, children: /* @__PURE__ */ jsx(ark.div, { ...mergedProps, ref }) });
});
ToggleGroup.displayName = "ToggleGroup";

export { ToggleGroup };

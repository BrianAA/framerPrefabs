'use client';
import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { forwardRef } from 'react';
import { createSplitProps } from '../create-split-props.mjs';
import { ark } from '../factory.mjs';
import { useSelectContext } from './select-context.mjs';

const SelectItemGroupLabel = forwardRef(
  (props, ref) => {
    const [itemGroupLabelProps, localProps] = createSplitProps()(props, [
      "htmlFor"
    ]);
    const api = useSelectContext();
    const mergedProps = mergeProps(api.getItemGroupLabelProps(itemGroupLabelProps), localProps);
    return /* @__PURE__ */ jsx(ark.div, { ...mergedProps, ref });
  }
);
SelectItemGroupLabel.displayName = "SelectItemGroupLabel";

export { SelectItemGroupLabel };

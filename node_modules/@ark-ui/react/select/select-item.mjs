'use client';
import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { forwardRef } from 'react';
import { createSplitProps } from '../create-split-props.mjs';
import { ark } from '../factory.mjs';
import { runIfFn } from '../run-if-fn.mjs';
import { useSelectContext } from './select-context.mjs';
import { SelectItemProvider } from './select-item-context.mjs';

const SelectItem = forwardRef((props, ref) => {
  const [itemProps, { children, ...localProps }] = createSplitProps()(props, ["item"]);
  const api = useSelectContext();
  const mergedProps = mergeProps(api.getItemProps(itemProps), localProps);
  const itemState = api.getItemState(itemProps);
  const view = runIfFn(children, itemState);
  return /* @__PURE__ */ jsx(SelectItemProvider, { value: itemProps, children: /* @__PURE__ */ jsx(ark.div, { ...mergedProps, ref, children: view }) });
});
SelectItem.displayName = "SelectItem";

export { SelectItem };

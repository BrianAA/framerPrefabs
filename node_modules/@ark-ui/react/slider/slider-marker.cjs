'use client';
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const react$1 = require('@zag-js/react');
const react = require('react');
const factory = require('../factory.cjs');
const sliderContext = require('./slider-context.cjs');

const SliderMarker = react.forwardRef((props, ref) => {
  const { value, ...spanProps } = props;
  const api = sliderContext.useSliderContext();
  const mergedProps = react$1.mergeProps(api.getMarkerProps({ value }), spanProps);
  return /* @__PURE__ */ jsxRuntime.jsx(factory.ark.span, { ...mergedProps, ref });
});
SliderMarker.displayName = "SliderMarker";

exports.SliderMarker = SliderMarker;

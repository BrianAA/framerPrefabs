'use client';
import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { forwardRef } from 'react';
import { createSplitProps } from '../create-split-props.mjs';
import { ark } from '../factory.mjs';
import { runIfFn } from '../run-if-fn.mjs';
import { SliderProvider } from './slider-context.mjs';
import { useSlider } from './use-slider.mjs';

const Slider = forwardRef((props, ref) => {
  const [useSliderProps, { children, ...divProps }] = createSplitProps()(props, [
    "aria-label",
    "aria-labelledby",
    "defaultValue",
    "dir",
    "disabled",
    "form",
    "getAriaValueText",
    "getRootNode",
    "id",
    "ids",
    "invalid",
    "max",
    "min",
    "minStepsBetweenThumbs",
    "name",
    "onFocusChange",
    "onValueChange",
    "onValueChangeEnd",
    "orientation",
    "origin",
    "readOnly",
    "step",
    "thumbAlignment",
    "thumbAlignment",
    "thumbSize",
    "value"
  ]);
  const api = useSlider(useSliderProps);
  const mergedProps = mergeProps(api.rootProps, divProps);
  const view = runIfFn(children, api);
  return /* @__PURE__ */ jsx(SliderProvider, { value: api, children: /* @__PURE__ */ jsx(ark.div, { ...mergedProps, ref, children: view }) });
});
Slider.displayName = "Slider";

export { Slider };

'use client';
import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { forwardRef } from 'react';
import { createSplitProps } from '../create-split-props.mjs';
import { ark } from '../factory.mjs';
import { runIfFn } from '../run-if-fn.mjs';
import { useRadioGroupContext } from './radio-group-context.mjs';
import { RadioGroupItemProvider } from './radio-group-item-context.mjs';

const RadioGroupItem = forwardRef((props, ref) => {
  const [itemProps, { children, ...localProps }] = createSplitProps()(props, [
    "value",
    "disabled",
    "invalid"
  ]);
  const api = useRadioGroupContext();
  const mergedProps = mergeProps(api.getItemProps(itemProps), localProps);
  const itemState = api.getItemState(itemProps);
  const view = runIfFn(children, itemState);
  return /* @__PURE__ */ jsx(RadioGroupItemProvider, { value: itemProps, children: /* @__PURE__ */ jsx(ark.label, { ...mergedProps, ref, children: view }) });
});
RadioGroupItem.displayName = "RadioGroupItem";

export { RadioGroupItem };

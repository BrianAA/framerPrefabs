'use client';
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const react$1 = require('@zag-js/react');
const react = require('react');
const createSplitProps = require('../create-split-props.cjs');
const factory = require('../factory.cjs');
const runIfFn = require('../run-if-fn.cjs');
const radioGroupContext = require('./radio-group-context.cjs');
const radioGroupItemContext = require('./radio-group-item-context.cjs');

const RadioGroupItem = react.forwardRef((props, ref) => {
  const [itemProps, { children, ...localProps }] = createSplitProps.createSplitProps()(props, [
    "value",
    "disabled",
    "invalid"
  ]);
  const api = radioGroupContext.useRadioGroupContext();
  const mergedProps = react$1.mergeProps(api.getItemProps(itemProps), localProps);
  const itemState = api.getItemState(itemProps);
  const view = runIfFn.runIfFn(children, itemState);
  return /* @__PURE__ */ jsxRuntime.jsx(radioGroupItemContext.RadioGroupItemProvider, { value: itemProps, children: /* @__PURE__ */ jsxRuntime.jsx(factory.ark.label, { ...mergedProps, ref, children: view }) });
});
RadioGroupItem.displayName = "RadioGroupItem";

exports.RadioGroupItem = RadioGroupItem;

'use client';
import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { forwardRef } from 'react';
import { ark } from '../factory.mjs';
import { useDatePickerContext } from './date-picker-context.mjs';

const DatePickerRangeText = forwardRef(
  (props, ref) => {
    const api = useDatePickerContext();
    const mergedProps = mergeProps(api.rangeTextProps, props);
    return /* @__PURE__ */ jsx(ark.div, { ...mergedProps, ref, children: api.visibleRangeText.start });
  }
);
DatePickerRangeText.displayName = "DatePickerRangeText";

export { DatePickerRangeText };

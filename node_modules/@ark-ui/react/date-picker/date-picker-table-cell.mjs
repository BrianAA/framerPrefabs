'use client';
import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { forwardRef } from 'react';
import { createSplitProps } from '../create-split-props.mjs';
import { ark } from '../factory.mjs';
import { useDatePickerContext } from './date-picker-context.mjs';
import { DatePickerTableCellProvider } from './date-picker-table-cell-context.mjs';
import { useDatePickerViewContext } from './date-picker-view-context.mjs';

const DatePickerTableCell = forwardRef(
  (props, ref) => {
    const [cellProps, localProps] = createSplitProps()(props, [
      "disabled",
      "value",
      "visibleRange",
      "columns"
    ]);
    const api = useDatePickerContext();
    const viewProps = useDatePickerViewContext();
    const tableCellProps = {
      day: api.getDayTableCellProps,
      month: api.getMonthTableCellProps,
      year: api.getYearTableCellProps
      // @ts-expect-error use filter guard
    }[viewProps.view](cellProps);
    const mergedProps = mergeProps(tableCellProps, localProps);
    return /* @__PURE__ */ jsx(DatePickerTableCellProvider, { value: cellProps, children: /* @__PURE__ */ jsx(ark.td, { ref, ...mergedProps }) });
  }
);
DatePickerTableCell.displayName = "DatePickerTableCell";

export { DatePickerTableCell };

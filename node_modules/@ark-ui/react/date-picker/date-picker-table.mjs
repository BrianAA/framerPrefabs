'use client';
import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { forwardRef, useId } from 'react';
import { createSplitProps } from '../create-split-props.mjs';
import { ark } from '../factory.mjs';
import { useDatePickerContext } from './date-picker-context.mjs';
import { DatePickerTableProvider } from './date-picker-table-context.mjs';
import { useDatePickerViewContext } from './date-picker-view-context.mjs';

const DatePickerTable = forwardRef((props, ref) => {
  const [{ columns }, localProps] = createSplitProps()(props, [
    "columns"
  ]);
  const api = useDatePickerContext();
  const viewProps = useDatePickerViewContext();
  const tableProps = { columns, id: useId(), ...viewProps };
  const mergedProps = mergeProps(api.getTableProps(tableProps), localProps);
  return /* @__PURE__ */ jsx(DatePickerTableProvider, { value: tableProps, children: /* @__PURE__ */ jsx(ark.table, { ...mergedProps, ref }) });
});
DatePickerTable.displayName = "DatePickerTable";

export { DatePickerTable };
